{
  "entities": {
    "SiteSettings": {
      "title": "SiteSettings",
      "description": "Global website settings that persist across sessions.",
      "type": "object",
      "properties": {
        "theme": {
          "type": "string",
          "description": "The current theme of the website."
        },
        "cameraSpeed": {
          "type": "number",
          "description": "The speed of the camera movements."
        }
      },
      "required": ["theme"]
    }
  },
  "firestore": {
    "/settings/global": {
      "schema": {
        "$ref": "SiteSettings"
      },
      "description": "Global settings document."
    }
  }
}
